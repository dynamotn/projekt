#!/bin/sh
# Build {{ .Name }} the way the Makefile does, for a CI runner without make.
set -eu

VERSION=$(git describe --tags --always --dirty 2>/dev/null || echo dev)
go build -ldflags "-X main.version=${VERSION}" -o "bin/{{ .Name }}" .
