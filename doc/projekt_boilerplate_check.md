## projekt boilerplate check

Validate a recipe, or the whole store

### Synopsis

Read a recipe the way creating from it would, and report what is wrong.

It parses the recipe without stopping at the first problem, resolves the
template it renders and checks that too, parses every `after` command line,
and looks at where the project would go and what it would point at.

Nothing is written and no project is created. Exits non-zero when there is an
error, so it can gate a CI job; warnings do not fail the check unless --strict
is given.

Examples:

  b check              # every recipe of the store
  b check go-cli       # one of them
  b check --strict

```
projekt boilerplate check [boilerplate] [flags]
```

### Options

```
  -h, --help     help for check
      --strict   Treat warnings as errors
```

### Options inherited from parent commands

```
      --boilerplate-dir string   Boilerplate folder (default is $XDG_DATA_HOME/projekt/boilerplates)
      --config string            config file (default is $XDG_CONFIG_HOME/projekt/config.yaml)
      --template-dir string      Template folder (default is $XDG_DATA_HOME/projekt/templates)
  -v, --verbose string           Log level, available options are: (trace, debug, info, warn, error, fatal) (default "info")
```

### SEE ALSO

* [projekt boilerplate](projekt_boilerplate.md)	 - Create boilerplate project folder of a language/framework/tool...

###### Auto generated by spf13/cobra on 25-Sep-2026
